# @admeta/cli

Install the admeta monetize Codex Skill, initialize a minimal configuration file, diagnose the supported Next.js + Vercel AI SDK path, and deterministically verify a completed integration.

The CLI does not replace Codex's semantic repository understanding and does not generate an application UI. The Codex Skill owns the integration decision; generated `SponsoredOffer` source remains owned by the app.
